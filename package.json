{
  "name": "matchery",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
	"heroku-postbuild": "cd matchery_client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^3.0.1",
    "dotenv": "^6.1.0",
    "express": "^4.16.2",
    "express-validator": "^5.3.0",
    "mongoose": "^5.2.16",
    "node-pre-gyp": "^0.11.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  },
  "engines": {
    "node": "8.12.0"
  }
}
